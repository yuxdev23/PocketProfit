/**
 * ข้อความคงที่ของผู้ช่วย PocketProfit (ภาษาไทย) — pure strings, ไม่มี server logic.
 *
 * แยกไฟล์นี้ออกจาก scope.ts (ที่เป็น "server-only") เพื่อให้ "ใช้ได้ทั้งฝั่ง client และ server":
 * client component (assistant-sheet) ต้องใช้ ERROR_FALLBACK ด้วย — ถ้า import จาก scope.ts
 * ที่มี import "server-only" จะทำให้ bundle ฝั่ง client พังทั้งแอป. ไฟล์นี้ปลอดภัยทุกฝั่ง.
 */

/** ปฏิเสธคำถามนอกขอบเขต (AI-4). */
export const OUT_OF_SCOPE =
  "ผมช่วยเรื่องการใช้งานและข้อมูลใน PocketProfit ได้ครับ เช่น กำไร งบประมาณ เป้าหมาย สินค้า หรือค่าใช้จ่ายประจำ ลองถามเรื่องพวกนี้ดูไหมครับ 🙂";

/** ผู้ใช้กด send โดยยังไม่พิมพ์ (AI-6). */
export const EMPTY_INPUT = "พิมพ์คำถามก่อนนะครับ";

/** ข้อความยาวเกินกำหนด (AI-6). */
export const TOO_LONG = "คำถามยาวเกินไปครับ ลองพิมพ์ให้สั้นลงหน่อยนะครับ";

/** โหมด A ล่ม + โหมด B ก็พัง (กันสุดท้าย) — ไม่ปล่อย raw 500 (AI-6). */
export const ERROR_FALLBACK =
  "ขออภัยครับ ตอนนี้ผู้ช่วยตอบไม่ได้ชั่วคราว ลองใหม่อีกครั้งนะครับ";

/** ถามเรื่องในขอบเขตแต่ยังไม่มีข้อมูลเดือนนี้ (AI-5) — กัน NaN และชวนเริ่มบันทึก. */
export const NO_DATA =
  "เดือนนี้ยังไม่มีข้อมูลเลยครับ ลองบันทึกรายการแรกจากปุ่ม + ก่อนนะครับ แล้วผมจะช่วยสรุปกำไร/งบให้ได้";
